# === УРОК 7: ФУНКЦИИ И ВОЗВРАТ ЗНАЧЕНИЙ (Расчет успешности тестов) ===
#
# В автотестах мы постоянно используем Функции. 
# Функция — это маленький мини-завод: на вход она получает сырье (аргументы), 
# делает с ними полезную работу и с помощью `return` отдает нам готовый результат наружу.
#
# ТВОЯ ЛОГИЧЕСКАЯ ЗАДАЧА:
# Написать функцию `get_pass_rate(passed, total)`, которая рассчитывает процент успешных тестов.
#
# Формула расчета процента: (passed / total) * 100
#
# План действий:
# 1. Объяви функцию с именем `get_pass_rate` и двумя параметрами в скобках: `passed` и `total`.
#    Не забудь двоеточие в конце строки: `def get_pass_rate(passed, total):`
# 2. Сделай отступ и создай переменную `rate`, в которую запиши результат расчета по формуле выше.
# 3. С помощью ключевого слова `return` верни этот `rate` наружу из функции.
#
# Пиши свой код ниже:

# (Код для проверки — раскомментируй его, когда напишешь функцию, чтобы проверить себя):
  # Должно вывести 80.0%
def get_pass_rate(passed, total):
    rate = (passed / total) * 100
    return rate
result = get_pass_rate(8, 10)
print(f"Процент успешных тестов: {result}%")