# === УРОК 3: СПИСКИ И ЦИКЛЫ ===
#
# В тестировании мы постоянно работаем со списками элементов: 
# списки товаров на сайте, списки пользователей и т.д. 
# Чтобы перебрать их все, используется цикл `for`.
#
# Задание:
# 1. Создай список `users` и запиши в него три имени: "Иван", "Анна" и "Петр" (обязательно в квадратных скобках `[]`!).
# 2. Напиши цикл `for`, чтобы перебрать этот список (используй конструкцию `for user in users:` с двоеточием в конце).
# 3. Внутри цикла (с отступом Tab) выведи приветствие для каждого пользователя через print() и f-строку: 
#    "Привет, [имя]!" (где вместо [имя] должно подставляться имя пользователя).
#
# Пиши свой код прямо ниже этой строки:
users = ["Иван","Анна","Петр"]
for user in users:
    print(f"Привет, {user}!")
