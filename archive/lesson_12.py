# Урок 4.2: Области видимости (Где живут переменные)
# ДУБЛЬ 2. Давай закрепим!

# 1. ЛОКАЛЬНЫЕ ПЕРЕМЕННЫЕ (Внутри функции)
# Переменная, созданная внутри функции, существует ТОЛЬКО внутри нее.
def my_secret():
    password = "123" 
    # Снаружи эту переменную password никто не увидит!

# 2. ГЛОБАЛЬНЫЕ ПЕРЕМЕННЫЕ (Снаружи функций)
user_name = "Артём" # Эта переменная видна всем в этом файле.

# 3. КАК ПРАВИЛЬНО ПЕРЕДАВАТЬ ДАННЫЕ В ФУНКЦИЮ
# Плохой способ (тот, что ты случайно использовал в прошлой попытке):
# Брать глобальную переменную прямо изнутри функции.
# def print_name():
#     print(user_name) # Работает, но если мы перенесем функцию в другой файл тестирования, она сломается!

# Хороший способ:
# Использовать АРГУМЕНТЫ (скобочки). Функция должна быть независимой!
# def print_name(name_to_print):
#     print(name_to_print)
#
# print_name(user_name) # Передаем глобальную переменную внутрь через "дверь" (аргумент)


# === ТВОЁ ЗАДАНИЕ ===
# Давай напишем функцию правильно, используя входной аргумент!
#
# 1. Раскомментируй нижние 5 строчек кода.
# 2. Добавь в скобки функции `show_profile` аргумент с любым названием (например, `username`).
# 3. Замени слово "ОШИБКА" в принте на этот аргумент.
# 4. При вызове функции (на последней строке) передай в её скобки глобальную переменную `my_name`.

# Пиши код ниже:


my_name = "Артём"  # Это глобальная переменная (живет снаружи)

# Добавили аргумент 'username' в скобки
def show_profile(username):
    # Заменили ОШИБКА на наш аргумент username
    print("Профиль пользователя:", username)

# Вызываем функцию и передаем в нее нашу глобальную переменную
show_profile(my_name)
